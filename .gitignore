*.iml
.gradle
/local.properties
.idea/
.vscode/
.DS_Store
build/
.kotlin/
/captures
.externalNativeBuild
.cxx
local.properties
secrets.properties
admob.properties

# Raw export from the device DB carries real GPS coordinates from actual drives — never commit it.
ml/data/raw_export.json

# Agent-internal scratch plan, not meant to be tracked (see .agent/.aiexclude).
.agent/plan.md

# Release bundles/APKs gerados localmente para a Play Store.
/app/release/
/app/google-services.json
