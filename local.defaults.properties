# Checked-in fallback values for the Secrets Gradle Plugin (used when secrets.properties
# is missing, e.g. on a fresh checkout or CI). Never put a real key here.
MAPS_API_KEY=DEFINE_MAPS_API_KEY_IN_SECRETS_PROPERTIES
