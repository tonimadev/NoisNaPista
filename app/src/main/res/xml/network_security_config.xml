<?xml version="1.0" encoding="utf-8"?>
<!--
  Allows plaintext HTTP only to the local dev backend (NoisNaPistaBackend running via
  `./gradlew bootRun` on the developer's machine). Every other destination still requires HTTPS.
  10.0.2.2 is the Android emulator's alias for the host machine's localhost; 127.0.0.1/localhost
  are included for `adb reverse` setups when testing on a physical device.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
